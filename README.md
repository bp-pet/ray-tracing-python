# Scene rendering in Python
Simple implementation of scene rendering. Can set a camera position and some objects in `main.py`, then running it will render the scene. Wanted to follow https://raytracing.github.io/books/RayTracingInOneWeekend.html but after reading a bit decided to first see how far I get without any other sources.

# TODO write assertions on array sizes